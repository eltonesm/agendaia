package com.simboraagendar.catalog.application.port.in;

import java.util.UUID;

/** O que sobrevive do cadastro de oferta para o adapter web mostrar. */
public record RegisteredServiceOffering(UUID id) {}
