<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>

	<parent>
		<groupId>com.agendaia</groupId>
		<artifactId>agendaia</artifactId>
		<version>0.0.1-SNAPSHOT</version>
	</parent>

	<artifactId>agendaia-bootstrap</artifactId>
	<name>AgendaIA :: Bootstrap</name>
	<description>Aplicação executável: composição, configuração e testes de arquitetura</description>

	<!--
		Único módulo que é uma aplicação Spring Boot (ADR 0001). Depende de todos
		os contextos; nenhum contexto depende dele.

		A classe @SpringBootApplication fica em com.agendaia, de modo que o
		component scan alcance com.agendaia.* em todos os jars de módulo.

		É também a casa dos testes que só fazem sentido sobre o sistema montado:
		ArchUnit e isolamento entre tenants.
	-->
	<dependencies>
		<dependency>
			<groupId>com.agendaia</groupId>
			<artifactId>agendaia-shared-kernel</artifactId>
			<version>${project.version}</version>
		</dependency>
		<dependency>
			<groupId>com.agendaia</groupId>
			<artifactId>agendaia-platform</artifactId>
			<version>${project.version}</version>
		</dependency>
		<dependency>
			<groupId>com.agendaia</groupId>
			<artifactId>agendaia-organization</artifactId>
			<version>${project.version}</version>
		</dependency>
		<dependency>
			<groupId>com.agendaia</groupId>
			<artifactId>agendaia-catalog</artifactId>
			<version>${project.version}</version>
		</dependency>
		<dependency>
			<groupId>com.agendaia</groupId>
			<artifactId>agendaia-scheduling</artifactId>
			<version>${project.version}</version>
		</dependency>
		<dependency>
			<groupId>com.agendaia</groupId>
			<artifactId>agendaia-customer</artifactId>
			<version>${project.version}</version>
		</dependency>

		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-webmvc</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-validation</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-actuator</artifactId>
		</dependency>

		<dependency>
			<groupId>org.postgresql</groupId>
			<artifactId>postgresql</artifactId>
			<scope>runtime</scope>
		</dependency>

		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-webmvc-test</artifactId>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-validation-test</artifactId>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-actuator-test</artifactId>
			<scope>test</scope>
		</dependency>
	</dependencies>

	<build>
		<plugins>
			<plugin>
				<groupId>org.springframework.boot</groupId>
				<artifactId>spring-boot-maven-plugin</artifactId>
			</plugin>
		</plugins>
	</build>

</project>
